const fs = require("fs"); const path = require("path"); const files = fs.readdirSync("public").filter(f => f.endsWith(".html")); files.forEach(f => { let p = path.join("public", f); let c = fs.readFileSync(p, "utf-8"); if (!c.includes("<div class=\"main-content\">")) { c = c.replace("</nav>", "</nav>\n  <div class=\"main-content\">"); c = c.replace("<footer class=\"footer\">", "</div>\n  <footer class=\"footer\">"); } c = c.replace(/<img(?![^>]*decoding="async"[^>]*)([^>]*)>/gi, "<img decoding=\"async\"$1>"); fs.writeFileSync(p, c); console.log("Updated " + f); });
